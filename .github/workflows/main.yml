name: main
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Lint code
        run: ./gradlew lint
      - name: Run tests
        run: ./gradlew test
